import 'package:flutter/material.dart';

class PersistentNavPadding extends StatelessWidget {
  const PersistentNavPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 10);
  }
}
